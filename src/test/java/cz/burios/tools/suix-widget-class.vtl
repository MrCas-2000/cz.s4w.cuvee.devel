package cz.burios.web.widget.suix.ui;

import cz.burios.web.widget.suix.Suix;

public class $md.name implements WidgetConfigDefs {
#foreach($field in $md.fields)
	
	/**
$field.comment
	*/
	public final static String $field.name = Suix.$field.name;
	#if($field.items.size() != 0)
	
	public class $field.name {
		#set($fields = $field.items)
		#parse("suix-widget-subclass.vtl")
	}
	#end
#end
}